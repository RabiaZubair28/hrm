<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <template id="reset_password" name="Force Password Reset">
        <t t-call="custom_login.force_password_layout">
            <div class="container mt-3 mb-5" style="max-width: 480px;">
                <div class="card shadow-lg" style="border-radius: 12px;">
                    <div class="card-body">

                        <h3 class="text-center mb-3">
                            Update Your Password 
                        </h3>

                        <p class="text-muted text-center mb-4">
                            For security reasons, you must update your password before continuing.
                        </p>
                        <t t-if="error">
                            <div class="alert alert-danger" role="alert">
                                <t t-esc="error"/>
                            </div>
                        </t>
                        <form id="passwordForm" action="/force_password_reset/submit" method="post">
                            <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>

                            <!-- Current Password -->
                            <div class="mb-3">
                                <label class="form-label">Current Password</label>
                                <div class="input-group">
                                    <input type="password"
                                           name="current_password"
                                           class="form-control password-field"
                                           required="required"/>
                                    <button type="button"
                                            class="btn btn-outline-secondary toggle-password"
                                            aria-label="Show/Hide password">
                                        <i class="fa fa-eye-slash" aria-hidden="true"></i>
                                    </button>
                                </div>
                            </div>

                            <!-- New Password -->
                            <div class="mb-3">
                                <label class="form-label">New Password</label>
                                <div class="input-group">
                                    <input type="password"
                                           id="new_password"
                                           name="new_password"
                                           class="form-control password-field"
                                           pattern="(?=.*\d)(?=.*[a-z])(?=.*[A-Z]).{8,}"
                                           title="Must contain at least 8 characters, including uppercase, lowercase and a number"
                                           required="required"/>
                                    <button type="button"
                                            class="btn btn-outline-secondary toggle-password"
                                            aria-label="Show/Hide password">
                                        <i class="fa fa-eye-slash" aria-hidden="true"></i>
                                    </button>
                                </div>
                                <small class="text-muted">
                                    Must be at least 8 characters, include uppercase, lowercase, and a number.
                                </small>
                            </div>

                            <!-- Confirm Password -->
                            <div class="mb-4">
                                <label class="form-label">Confirm New Password</label>
                                <div class="input-group">
                                    <input type="password"
                                           id="confirm_password"
                                           name="confirm_password"
                                           class="form-control password-field"
                                           required="required"/>
                                    <button type="button"
                                            class="btn btn-outline-secondary toggle-password"
                                            aria-label="Show/Hide password">
                                        <i class="fa fa-eye-slash" aria-hidden="true"></i>
                                    </button>
                                </div>
                            </div>

                            <div id="passwordMatchMsg" class="small mt-1"></div>

                            <div class="d-grid">
                                <button type="submit" class="btn btn-primary" style="background-color: #064526;">
                                    Update Password
                                </button>
                            </div>
                        </form>

                    </div>
                </div>
            </div>
        </t>
    </template>
</odoo>
